# CentaurAI 多用户管理 Implementation Plan

> **For Hermes:** Implement task-by-task. Each task is 2-5 min.

**Goal:** 在 Settings 中添加「用户管理」标签页，admin 可以创建/删除局域网用户，每人独立登录看到自己的会话。

**Architecture:** Electron main process (Node.js) 通过 bcryptjs 哈希密码后直接操作 SQLite，前端通过 IPC bridge 调用。不依赖 Rust 后端改动。

**Tech Stack:** React + Arco Design + bcryptjs (已有) + better-sqlite3 (已有)

---

## 架构设计

```
Renderer (React)          Main Process (Node.js)         SQLite
─────────────────         ─────────────────────         ──────
UsersModalContent.tsx     userManagementBridge.ts        aionui-backend.db
  ├─ listUsers()  ──IPC──→  SELECT * FROM users           users 表
  ├─ createUser() ──IPC──→  bcryptjs.hash() → INSERT
  ├─ deleteUser() ──IPC──→  DELETE FROM users (CASCADE)
  └─ resetPwd()   ──IPC──→  bcryptjs.hash() → UPDATE
```

所有 CRUD 操作都在 main process 端完成（已有 DB 连接），不依赖后端 HTTP API。
仅 `listUsers()` 复用后端 `GET /api/auth/internal/users` 获取用户列表（带 jwt_secret 等完整字段）。

---

## Task 1: 添加 IPC bridge 类型定义

**Objective:** 在 ipcBridge.ts 中声明用户管理相关的 IPC 通道

**Files:**

- Modify: `packages/desktop/src/common/adapter/ipcBridge.ts`

**Step 1: 在 webui export 之后添加 users 模块**

在 `ipcBridge.ts` 的 `webui` 定义（约 line 1244）之后，`cron` 之前，添加：

```typescript
// ---------------------------------------------------------------------------
// User Management — main-process IPC (direct DB access via bcryptjs)
// ---------------------------------------------------------------------------

export type IUserRecord = {
  id: string;
  username: string;
  email: string | null;
  avatar_path: string | null;
  created_at: number;
  updated_at: number;
  last_login: number | null;
};

export type ICreateUserParams = {
  username: string;
  password: string;
};

export type IResetUserPasswordResult = {
  new_password: string;
};

export const users = {
  list: bridge.buildProvider<IUserRecord[], void>('users.list'),
  create: bridge.buildProvider<IUserRecord, ICreateUserParams>('users.create'),
  delete: bridge.buildProvider<void, { user_id: string }>('users.delete'),
  resetPassword: bridge.buildProvider<IResetUserPasswordResult, { user_id: string }>('users.reset-password'),
};
```

---

## Task 2: 创建用户管理 Bridge（Main Process）

**Objective:** 在 main process 实现用户 CRUD 的 IPC handler

**Files:**

- Create: `packages/desktop/src/process/bridge/userManagementBridge.ts`
- Modify: `packages/desktop/src/process/bridge/index.ts`

**Step 1: 创建 userManagementBridge.ts**

```typescript
/**
 * @license
 * Copyright 2025 AionUi (aionui.com)
 * SPDX-License-Identifier: Apache-2.0
 *
 * User management bridge for the desktop settings page.
 * Operations go through the backend's internal HTTP API where available
 * (list/create), and fall back to direct SQLite access for delete/reset.
 */

import { ipcBridge, type IUserRecord, type ICreateUserParams, type IResetUserPasswordResult } from '@/common';
import BetterSqlite3 from 'better-sqlite3';
import bcrypt from 'bcryptjs';
import { randomBytes } from 'crypto';
import { getDataPath } from '@process/utils/utils';
import path from 'path';

const BCRYPT_ROUNDS = 12;

function openDb(): BetterSqlite3.Database {
  const dbPath = path.join(getDataPath(), 'aionui', 'aionui-backend.db');
  const db = new BetterSqlite3(dbPath);
  db.pragma('busy_timeout = 5000');
  return db;
}

function getBackendPort(): number | undefined {
  return (globalThis as typeof globalThis & { __backendPort?: number }).__backendPort;
}

export function initUserManagementBridge(): void {
  // ── list ──────────────────────────────────────────────────────
  ipcBridge.users.list.provider(async () => {
    const port = getBackendPort();
    if (port) {
      try {
        const res = await fetch(`http://127.0.0.1:${port}/api/auth/internal/users`);
        if (res.ok) {
          const json = (await res.json()) as { success?: boolean; data?: IUserRecord[] };
          if (json.success && Array.isArray(json.data)) {
            // Strip sensitive fields before sending to renderer
            return json.data.map(({ password_hash, jwt_secret, ...rest }) => rest as IUserRecord);
          }
        }
      } catch {
        /* fall through to DB */
      }
    }

    // Fallback: direct DB query
    const db = openDb();
    try {
      const rows = db
        .prepare(
          'SELECT id, username, email, avatar_path, created_at, updated_at, last_login FROM users ORDER BY created_at'
        )
        .all() as IUserRecord[];
      return rows;
    } finally {
      db.close();
    }
  });

  // ── create ────────────────────────────────────────────────────
  ipcBridge.users.create.provider(async (params: ICreateUserParams) => {
    const port = getBackendPort();
    if (!port) throw new Error('Backend not running');

    const passwordHash = bcrypt.hashSync(params.password, BCRYPT_ROUNDS);

    const res = await fetch(`http://127.0.0.1:${port}/api/auth/internal/users`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        username: params.username,
        password_hash: passwordHash,
      }),
    });

    if (!res.ok) {
      const err = (await res.json().catch(() => ({}))) as { error?: string };
      throw new Error(err.error || `Failed to create user (${res.status})`);
    }

    const json = (await res.json()) as { success?: boolean; data?: IUserRecord };
    if (!json.success || !json.data) {
      throw new Error('Failed to create user');
    }

    // Strip sensitive fields
    const {
      password_hash: _,
      jwt_secret: __,
      ...safe
    } = json.data as IUserRecord & { password_hash?: string; jwt_secret?: string };
    return safe as IUserRecord;
  });

  // ── delete ────────────────────────────────────────────────────
  ipcBridge.users.delete.provider(async (params: { user_id: string }) => {
    const db = openDb();
    try {
      // SQLite ON DELETE CASCADE handles conversations/messages/teams
      const result = db.prepare('DELETE FROM users WHERE id = ?').run(params.user_id);
      if (result.changes === 0) {
        throw new Error('User not found');
      }
    } finally {
      db.close();
    }
  });

  // ── resetPassword ─────────────────────────────────────────────
  ipcBridge.users.resetPassword.provider(async (params: { user_id: string }) => {
    const newPassword = randomBytes(8).toString('hex'); // 16-char hex
    const passwordHash = bcrypt.hashSync(newPassword, BCRYPT_ROUNDS);

    const db = openDb();
    try {
      const result = db
        .prepare('UPDATE users SET password_hash = ?, updated_at = ? WHERE id = ?')
        .run(passwordHash, Date.now(), params.user_id);

      if (result.changes === 0) {
        throw new Error('User not found');
      }

      // Also update jwt_secret to invalidate existing sessions
      const newJwtSecret = randomBytes(32).toString('base64');
      db.prepare('UPDATE users SET jwt_secret = ? WHERE id = ?').run(newJwtSecret, params.user_id);

      return { new_password: newPassword };
    } finally {
      db.close();
    }
  });
}
```

**Step 2: 注册到 index.ts**

在 `packages/desktop/src/process/bridge/index.ts` 中：

- import 行添加：`import { initUserManagementBridge } from './userManagementBridge';`
- `initAllBridges()` 中添加：`initUserManagementBridge();`
- export 列表中添加：`initUserManagementBridge,`

---

## Task 3: 创建前端用户管理组件

**Objective:** 在 Settings Modal 中添加用户管理标签页

**Files:**

- Create: `packages/desktop/src/renderer/components/settings/SettingsModal/contents/UsersModalContent.tsx`
- Modify: `packages/desktop/src/renderer/components/settings/SettingsModal/index.tsx`

**Step 1: 创建 UsersModalContent.tsx**

完整组件代码：

```tsx
/**
 * @license
 * Copyright 2025 AionUi (aionui.com)
 * SPDX-License-Identifier: Apache-2.0
 */

import { ipcBridge, type IUserRecord } from '@/common';
import { Button, Message, Modal, Popconfirm, Table, Tag, Typography } from '@arco-design/web-react';
import { Delete, Plus, Refresh, User } from '@icon-park/react';
import React, { useCallback, useEffect, useState } from 'react';
import { useTranslation } from 'react-i18next';

const { Text } = Typography;

type CreateUserFormData = {
  username: string;
  password: string;
};

const UsersModalContent: React.FC = () => {
  const { t } = useTranslation();
  const [users, setUsers] = useState<IUserRecord[]>([]);
  const [loading, setLoading] = useState(false);
  const [createVisible, setCreateVisible] = useState(false);
  const [createLoading, setCreateLoading] = useState(false);
  const [newUsername, setNewUsername] = useState('');
  const [newPassword, setNewPassword] = useState('');

  const loadUsers = useCallback(async () => {
    setLoading(true);
    try {
      const data = await ipcBridge.users.list.invoke();
      setUsers(data);
    } catch (err) {
      console.error('Failed to load users:', err);
      Message.error(t('settings.users.loadFailed'));
    } finally {
      setLoading(false);
    }
  }, [t]);

  useEffect(() => {
    void loadUsers();
  }, [loadUsers]);

  const handleCreate = async () => {
    if (!newUsername.trim()) {
      Message.warning(t('settings.users.usernameRequired'));
      return;
    }
    if (!newPassword || newPassword.length < 6) {
      Message.warning(t('settings.users.passwordMinLength'));
      return;
    }

    setCreateLoading(true);
    try {
      await ipcBridge.users.create.invoke({
        username: newUsername.trim(),
        password: newPassword,
      });
      Message.success(t('settings.users.createSuccess'));
      setCreateVisible(false);
      setNewUsername('');
      setNewPassword('');
      void loadUsers();
    } catch (err) {
      const msg = err instanceof Error ? err.message : t('settings.users.createFailed');
      Message.error(msg);
    } finally {
      setCreateLoading(false);
    }
  };

  const handleDelete = async (userId: string, username: string) => {
    try {
      // Don't allow deleting the admin user
      if (username === 'admin') {
        Message.warning(t('settings.users.cannotDeleteAdmin'));
        return;
      }
      await ipcBridge.users.delete.invoke({ user_id: userId });
      Message.success(t('settings.users.deleteSuccess'));
      void loadUsers();
    } catch (err) {
      const msg = err instanceof Error ? err.message : t('settings.users.deleteFailed');
      Message.error(msg);
    }
  };

  const handleResetPassword = async (userId: string) => {
    try {
      const result = await ipcBridge.users.resetPassword.invoke({ user_id: userId });
      Message.success({
        content: `${t('settings.users.passwordReset')}: ${result.new_password}`,
        duration: 10000,
      });
    } catch (err) {
      const msg = err instanceof Error ? err.message : t('settings.users.resetFailed');
      Message.error(msg);
    }
  };

  const formatDate = (ts: number | null) => {
    if (!ts) return '-';
    return new Date(ts).toLocaleString();
  };

  const columns = [
    {
      title: t('settings.users.username'),
      dataIndex: 'username',
      key: 'username',
      render: (name: string, record: IUserRecord) => (
        <span className='flex items-center gap-8px'>
          <User theme='outline' size='16' />
          <Text>{name}</Text>
          {record.id === 'system_default_user' && (
            <Tag color='arcoblue' size='small'>
              {t('settings.users.admin')}
            </Tag>
          )}
        </span>
      ),
    },
    {
      title: t('settings.users.createdAt'),
      dataIndex: 'created_at',
      key: 'created_at',
      render: (ts: number) => formatDate(ts),
    },
    {
      title: t('settings.users.lastLogin'),
      dataIndex: 'last_login',
      key: 'last_login',
      render: (ts: number | null) => formatDate(ts),
    },
    {
      title: t('settings.users.actions'),
      key: 'actions',
      render: (_: unknown, record: IUserRecord) => (
        <div className='flex items-center gap-8px'>
          <Button type='text' size='small' onClick={() => handleResetPassword(record.id)}>
            {t('settings.users.resetPassword')}
          </Button>
          {record.username !== 'admin' && (
            <Popconfirm title={t('settings.users.deleteConfirm')} onOk={() => handleDelete(record.id, record.username)}>
              <Button type='text' size='small' status='danger' icon={<Delete />}>
                {t('settings.users.delete')}
              </Button>
            </Popconfirm>
          )}
        </div>
      ),
    },
  ];

  return (
    <div className='flex flex-col h-full'>
      <div className='flex items-center justify-between mb-16px'>
        <div>
          <h3 className='text-16px font-600 text-t-primary m-0'>{t('settings.users.title')}</h3>
          <p className='text-12px text-t-tertiary mt-4px m-0'>{t('settings.users.description')}</p>
        </div>
        <div className='flex items-center gap-8px'>
          <Button icon={<Refresh />} onClick={loadUsers} loading={loading}>
            {t('settings.users.refresh')}
          </Button>
          <Button type='primary' icon={<Plus />} onClick={() => setCreateVisible(true)}>
            {t('settings.users.createUser')}
          </Button>
        </div>
      </div>

      <Table
        columns={columns}
        data={users}
        loading={loading}
        rowKey='id'
        size='small'
        pagination={false}
        className='flex-1'
      />

      <Modal
        visible={createVisible}
        onCancel={() => {
          setCreateVisible(false);
          setNewUsername('');
          setNewPassword('');
        }}
        title={t('settings.users.createUser')}
        onOk={handleCreate}
        confirmLoading={createLoading}
      >
        <div className='flex flex-col gap-16px py-8px'>
          <div>
            <label className='text-14px text-t-primary mb-4px block'>{t('settings.users.username')}</label>
            <input
              className='w-full h-36px px-12px rd-6px b-1 b-color-border-2 text-14px outline-none focus:b-color-primary'
              placeholder={t('settings.users.usernamePlaceholder')}
              value={newUsername}
              onChange={(e) => setNewUsername(e.target.value)}
            />
          </div>
          <div>
            <label className='text-14px text-t-primary mb-4px block'>{t('settings.users.password')}</label>
            <input
              type='password'
              className='w-full h-36px px-12px rd-6px b-1 b-color-border-2 text-14px outline-none focus:b-color-primary'
              placeholder={t('settings.users.passwordPlaceholder')}
              value={newPassword}
              onChange={(e) => setNewPassword(e.target.value)}
            />
          </div>
        </div>
      </Modal>
    </div>
  );
};

export default UsersModalContent;
```

**Step 2: 修改 SettingsModal/index.tsx**

1. 在 import 区域添加：

```typescript
import UsersModalContent from './contents/UsersModalContent';
```

2. 在 `BuiltinSettingTab` 类型中添加 `'users'`：

```typescript
export type BuiltinSettingTab = 'model' | 'agent' | 'tools' | 'webui' | 'system' | 'about' | 'users';
```

3. 在 `menuItems` 中添加（在 webui 之后，system 之前）：

```typescript
if (isDesktop) {
  builtinItems.push({
    key: 'webui',
    label: t('settings.webui'),
    icon: <Earth theme='outline' size='20' fill={iconColors.secondary} />,
  });
  builtinItems.push({
    key: 'users',
    label: t('settings.users'),
    icon: <User theme='outline' size='20' fill={iconColors.secondary} />,
  });
}
```

4. 在 `renderBuiltinContent` switch 中添加：

```typescript
case 'users':
  return <UsersModalContent />;
```

5. 需要导入 User 图标。在已有 icon-park import 中添加 `User`：

```typescript
import { Computer, Earth, Info, LinkCloud, Puzzle, Toolkit, User } from '@icon-park/react';
```

---

## Task 4: 添加 i18n 翻译键

**Objective:** 为所有语言添加用户管理相关的翻译

**Files:**

- Modify: `packages/desktop/src/renderer/services/i18n/locales/zh-CN/settings.json`
- Modify: `packages/desktop/src/renderer/services/i18n/locales/en-US/settings.json`

**Step 1: 在 zh-CN/settings.json 的 `"webui"` section 之后添加：**

```json
"users": "用户管理",
"users.title": "用户管理",
"users.description": "管理局域网用户的账户，每个用户登录后只能看到自己的会话",
"users.username": "用户名",
"users.usernamePlaceholder": "请输入用户名",
"users.password": "密码",
"users.passwordPlaceholder": "请输入密码（至少6位）",
"users.usernameRequired": "请输入用户名",
"users.passwordMinLength": "密码至少6位",
"users.admin": "管理员",
"users.createdAt": "创建时间",
"users.lastLogin": "最后登录",
"users.actions": "操作",
"users.refresh": "刷新",
"users.createUser": "新建用户",
"users.resetPassword": "重置密码",
"users.delete": "删除",
"users.deleteConfirm": "确定要删除此用户吗？其所有会话和数据将被清除。",
"users.createSuccess": "用户创建成功",
"users.createFailed": "用户创建失败",
"users.deleteSuccess": "用户已删除",
"users.deleteFailed": "删除用户失败",
"users.cannotDeleteAdmin": "不能删除管理员账户",
"users.passwordReset": "密码已重置，新密码为",
"users.resetFailed": "重置密码失败",
"users.loadFailed": "加载用户列表失败",
```

**Step 2: 在 en-US/settings.json 中对应添加英文翻译**

---

## Task 5: 验证

**Objective:** 运行 lint + typecheck + test 确保改动无误

**Files:** None (验证步骤)

**Step 1: 检查 TypeScript 类型**

```bash
cd /home/user/桌面/centaurai-aionui/packages/desktop
bunx tsc --noEmit 2>&1 | head -50
```

**Step 2: 运行 lint**

```bash
cd /home/user/桌面/centaurai-aionui
bun run lint:fix
bun run format
```

**Step 3: 运行测试**

```bash
cd /home/user/桌面/centaurai-aionui
bun run test
```

**Step 4: 构建测试**

```bash
cd /home/user/桌面/centaurai-aionui
bun run build
```

---

## 风险 & 注意事项

1. **bcryptjs 同步调用** — `hashSync` 在 main process 中是同步的，对于单次用户创建足够快（<100ms），不会阻塞 UI
2. **admin 不可删除** — 前端 + 后端都做了保护（前端检查 username !== 'admin'，建议后端也加上保护）
3. **ON DELETE CASCADE** — SQLite schema 已配置外键级联删除，删除用户会自动清理其 conversations/messages/teams
4. **会话失效** — 重置密码时会生成新的 jwt_secret，该用户的所有现有登录会话会立即失效
5. **类型生成** — `i18n-keys.d.ts` 是自动生成的，需要运行 `bun run i18n:types` 才能更新类型
